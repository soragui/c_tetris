# 第 4 课：游戏循环 🔄

## 4.1 游戏循环流程

```
处理输入 → 更新状态 → 渲染画面 → 控制速度 → 循环
```

## 4.2 代码实现

```c
int main(void) {
    render_init();
    Game* game = game_create();
    game_init(game);
    
    while (game->running) {
        input_handle_game(game, input_get_key());
        game_update(game);
        game_render(game);
        napms(50);
    }
    
    game_destroy(game);
    render_cleanup();
    return 0;
}
```

## 4.3 速度控制

- 初始速度：3000ms/格（3 秒）
- 每级加快：10%
- 最快速度：300ms/格

## ✅ 本课检查清单

- [ ] 理解游戏循环的概念
- [ ] 掌握输入 - 更新 - 渲染流程
- [ ] 理解方块下落逻辑
- [ ] 知道如何控制游戏速度

---

下一课：[碰撞检测](05-碰撞检测.md)
