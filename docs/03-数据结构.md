# 第 3 课：数据结构 🏗️

## 3.1 方块（Tetromino）

7 种标准方块：I, O, T, S, Z, J, L

```c
typedef struct {
    TetrominoType type;     // 方块类型
    int x, y;               // 位置
    int rotation;           // 旋转状态 (0-3)
    int color;              // 颜色
    int shape[4][4];        // 4x4 形状矩阵
} Tetromino;
```

## 3.2 游戏板（Board）

```c
typedef struct {
    int cells[BOARD_HEIGHT][BOARD_WIDTH];  // 0=空，1-7=颜色
} Board;
```

## 3.3 游戏状态（Game）

```c
typedef struct {
    Board board;
    Tetromino current, next, hold;
    int score, level, lines_cleared;
    bool running, paused, game_over;
} Game;
```

## ✅ 本课检查清单

- [ ] 理解 7 种方块类型
- [ ] 知道为什么用 4x4 矩阵
- [ ] 理解游戏板的数组结构
- [ ] 掌握 Game 结构体的组成

---

下一课：[游戏循环](04-游戏循环.md)
