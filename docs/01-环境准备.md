# 第 1 课：环境准备 🛠️

## 1.1 安装必要的软件

### Ubuntu/Debian 系统

```bash
sudo apt update
sudo apt install build-essential gcc make gdb libncurses5-dev
```

### Fedora/CentOS/RHEL 系统

```bash
sudo dnf groupinstall "Development Tools"
sudo dnf install ncurses-devel
```

## 1.2 验证安装

```bash
gcc --version
make --version
```

## 1.3 编译项目

```bash
cd ~/Work/open_learn/c_proj/tetris
make          # 编译
make run      # 运行
make clean    # 清理
```

## ✅ 本课检查清单

- [ ] 已安装 GCC 编译器
- [ ] 已安装 Make 工具
- [ ] 已安装 ncurses 库
- [ ] 能成功编译项目

---

下一课：[项目结构](02-项目结构.md)
