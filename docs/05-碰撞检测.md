# 第 5 课：碰撞检测 🚧

## 5.1 检测条件

1. 超出左边界：`x < 0`
2. 超出右边界：`x >= BOARD_WIDTH`
3. 超出底边界：`y >= BOARD_HEIGHT`
4. 碰到已固定方块：`board[y][x] != 0`

## 5.2 代码实现

```c
bool check_collision(Tetromino* t, Board* board) {
    for (int i = 0; i < 4; i++) {
        for (int j = 0; j < 4; j++) {
            if (t->shape[i][j]) {
                int x = t->x + j;
                int y = t->y + i;
                
                if (x < 0 || x >= BOARD_WIDTH || y >= BOARD_HEIGHT)
                    return true;
                if (y >= 0 && board->cells[y][x])
                    return true;
            }
        }
    }
    return false;
}
```

## ✅ 本课检查清单

- [ ] 理解碰撞检测的原理
- [ ] 知道需要检测哪些边界
- [ ] 掌握碰撞检测的代码实现

---

下一课：[方块旋转](06-方块旋转.md)
